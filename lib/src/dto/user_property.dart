enum QUserProperty {
  email,
  name,
  kochavaDeviceId,
  appsFlyerUserId,
  adjustAdId,
  customUserId,
  facebookAttribution,
  firebaseAppInstanceId,
  appSetId, // Android only
  advertisingId, // iOS only
}
